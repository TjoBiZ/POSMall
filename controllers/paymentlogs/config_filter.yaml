scopes:
    failed:
        label: 'kodzero.posmall::lang.payment_log.failed_only'
        type: checkbox
        default: 0
        conditions: failed = 1
    created_at:
        label: 'kodzero.posmall::lang.common.created_at'
        type: daterange
        conditions: created_at >= ':after' AND created_at <= ':before'
